import { StyleSheet } from 'react-native';
import { Colors } from '@/themes/colors';

export const charCounterStyles1 = StyleSheet.create({
  counter: {
    alignSelf: 'flex-end',
    fontSize: 12,
    fontFamily: 'interRegular',
    color: Colors.gray3,
    marginRight: 4,
    marginTop: 5
  },
});