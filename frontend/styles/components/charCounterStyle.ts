import { StyleSheet } from 'react-native';
import { Colors } from '@/themes/colors';

export const charCounterStyles = StyleSheet.create({
  counter: {
    alignSelf: 'flex-end',
    marginTop: 4,
    fontSize: 12,
    color: Colors.violet5,
    marginRight: 4,
  },
});